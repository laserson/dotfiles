# homebrew
export PATH="/usr/local/bin:$PATH"
export PATH="/usr/local/sbin:$PATH"

# updated gnu tools
export PATH="$(brew --prefix coreutils)/libexec/gnubin:$PATH"

# conda
# this is the proper conda bootstrap, but it's slow
#. /Users/laserson/miniconda3/etc/profile.d/conda.sh
#conda activate
# so instead use the originally-recommended
export PATH="/Users/laserson/miniconda3/bin:$PATH"

export MANPATH="$(brew --prefix coreutils)/libexec/gnuman:$MANPATH"

export PATH="$PATH:$(go env GOPATH)/bin"
